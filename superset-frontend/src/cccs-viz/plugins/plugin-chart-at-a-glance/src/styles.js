const Datum = {
    border: '1px solid',
    borderColor:'#20A7C9',
    fontSize: "14px",
    marginBottom: 2,
    padding: 2
  }

  const DatumLink = {
    marginTop: 1
  }

  const SectionTitle = {
    display: 'flex',
    alignItems: 'center',
    fontSize: "22px",
    padding: 1,
    spacing: 1
  }

  const UrlList = {
    hight: '100%',
    width: '100%',
    maxWidth: 360,
    overflow: 'auto',
    maxHeight: 100,
    float: 'left',
  }

  const UrlListItem = {
    backgroundColor: 'inherit',
    padding: 0,
  }

  const HostList = {
    maxHeight: 300,
    overflow: 'scroll',
  }

  const styles = {
    Datum: Datum,
    DatumLink: DatumLink,
    SectionTitle: SectionTitle,
    UrlList: UrlList,
    UrlListItem: UrlListItem,
    HostList: HostList,
  }

  export default styles;
  