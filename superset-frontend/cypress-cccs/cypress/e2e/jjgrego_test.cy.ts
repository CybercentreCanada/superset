import { DEPARTMENTS_DATASET, 
  COMPLEX_STRUCTURES_DATASET, 
  IP_ADDRESSES_DATASET, 
  EMPLOYEES_DATASET, 
  cccsDefaults,
  cccsExploreView,
  baseFormData,
  DATASET_URL,
  CHART_DATA_URL,
  SQLLAB_URL } from '../support/helper'
import { exploreView, sqlLabView } from '../support/directories'
import { chooseSelectDropdownOption } from '@hon2a/cypress-antd'
import { ipv4ToIntAsString } from  '../support/helper';

before(() => {

});

describe('Various tests', () => {

  it('Make a REST call to Datahub', () => {

  })

})
